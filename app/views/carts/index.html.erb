<p style="color: green"><%= notice %></p>

<h1>Carts</h1>
<%= link_to "New cart", new_cart_path, class: 'btn btn-primary mb-3' %>

<%= turbo_stream_from "carts" %>
<%= turbo_frame_tag "carts" do %>
  <% @carts.each do |cart| %>
    <%= render cart %>
  <% end %>
<% end %>

